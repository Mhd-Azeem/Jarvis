name: Build Jarvis

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Jarvis release workflow temporarily paused while Windows support is added."
